const reviews = [{
    content: "A towering masterpiece.",
    bookId: -1
}];

export const reviewsReducer = (state = reviews, action) => {
    switch (action.type) {
        default:
            return state
    }
}